#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int a = 97;

	while (a <= 122)
	{
		putchar(a);
		a++;
	}
	a = 65;
	while (a <= 90)
	{
		putchar(a);
		a++;
	}
return (0);
}
